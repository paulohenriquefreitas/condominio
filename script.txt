Create database Condominio;

use Condominio;

Create table Modelo(
CPF int (11)  not null,
Nome varchar (25) not null,
Ap int(3) primary key,
Estado varchar (25)
);
 
Create table Fornecedor(
 Nome varchar (99) not null
 
 );
 